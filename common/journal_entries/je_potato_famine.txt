je_potato_famine = {

 icon = "gfx/interface/icons/event_icons/event_skull.dds"

    group = je_group_historical_content

	is_shown_in_lobby = {
		c:GBR ?= this
	}

    is_shown_when_inactive = {
		c:GBR ?= this
	}

    possible = {
        c:GBR = this
		custom_tooltip = {
			text = hungry_forties_triggered_britain_tt
			has_global_variable = hungry_forties_var
		}
    }

    immediate = {
		trigger_event = { id = potato_famine_start.1 }
		activate_law = law_type:law_no_migration_controls
    }

    complete = {
        has_law = law_type:law_free_trade
		has_law = law_type:law_poor_laws
		OR = {
			has_law = law_type:law_cultural_exclusion
			has_law = law_type:law_multicultural
		}
		OR = {
			has_law = law_type:law_census_voting
			has_law = law_type:law_universal_suffrage
		}
        has_any_subventions_on = g:grain
		market_capital.market = {
			mg:grain = {
				market_goods_cheaper > 0
			}
		}
    }

	
	
	

    on_complete = {
		activate_law = law_type:law_migration_controls

		every_scope_state = {
			limit = {
			has_modifier = modifier_potato_famine
			}
			remove_modifier = modifier_potato_famine
		}
	}

	on_yearly_pulse = {
		random_events = {
			10 = 0
			10 = potato_famine_start.2
		}
	}

    fail = {
		NOT = {
			owns_entire_state_region = STATE_CONNAUGHT
			owns_entire_state_region = STATE_LEINSTER
			owns_entire_state_region = STATE_MUNSTER
		}
	}
    
    on_fail = {
		activate_law = law_type:law_migration_controls

		every_scope_state = {
			limit = {
			has_modifier = modifier_potato_famine
			}
			remove_modifier = modifier_potato_famine
		}
	}
           
    on_timeout = {
		activate_law = law_type:law_migration_controls

		every_scope_state = {
			limit = {
			has_modifier = modifier_potato_famine
			}
			remove_modifier = modifier_potato_famine
		}
	}
    
    invalid = {
    }

    should_be_pinned_by_default_uninvolved_or_context = yes

    timeout = 2555

    weight = 100
}